<h1>All Users</h1>
<% @users.each do |user| %>
  <div>
    <%= link_to user_path(user), class: 'users-links' do %>
      <%= render partial: "shared/card", locals: { user: user } %>
    <% end %>
  </div>
<% end %>